"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.generateCreativeContent = void 0;
const openai_1 = __importDefault(require("openai"));
// Initialize OpenAI Client
const openai = new openai_1.default({
    apiKey: process.env.OPENAI_API_KEY || 'mock-key',
});
const generateCreativeContent = (platform, topic, tone, additionalInstructions) => __awaiter(void 0, void 0, void 0, function* () {
    if (!process.env.OPENAI_API_KEY) {
        console.warn('No OpenAI API Key found. Returning mock content.');
        return mockContentGeneration(platform, topic, tone);
    }
    try {
        const completion = yield openai.chat.completions.create({
            messages: [
                {
                    role: 'system',
                    content: `You are an expert social media marketer. Generate a ${tone} post for ${platform}.`
                },
                {
                    role: 'user',
                    content: `Topic: ${topic}\nAdditional Instructions: ${additionalInstructions || 'None'}`
                }
            ],
            model: 'gpt-4',
        });
        return completion.choices[0].message.content;
    }
    catch (error) {
        console.error('Error generating content:', error);
        throw new Error('Failed to generate content');
    }
});
exports.generateCreativeContent = generateCreativeContent;
const mockContentGeneration = (platform, topic, tone) => {
    return `[MOCK ${platform} POST]\n\nTopic: ${topic}\nTone: ${tone}\n\nHere is a creative caption that would normally be generated by AI. It includes emojis 🚀 and hashtags #growth #marketing.`;
};
